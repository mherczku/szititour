import { ConvertToActivePlacePipe } from './convert-to-active-place.pipe';

describe('ConvertToActivePlacePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToActivePlacePipe();
    expect(pipe).toBeTruthy();
  });
});
